namespace CrazyEights.Domain;
/// <summary>
/// Using "enum" serves as a fixed set of named values.
/// If using numbers, be sure to specify what number to begin with, else Two begins with 0...
/// </summary>

public enum Rank
{
    Two = 2,
    Three,
    Four,
    Five,
    Six,
    Seven,
    Eight,
    Nine,
    Ten,
    Jack,
    Queen,
    King,
    Ace,
}