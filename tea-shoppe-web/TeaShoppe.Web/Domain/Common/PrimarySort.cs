namespace TeaShoppe.Web.Domain.Common;

/// <summary>
/// Enum representing the primary sort selection.
/// Note: Both cannot be primary / secondary at the same time.
/// </summary>

public enum PrimarySort
{
    Price,
    Rating
}
